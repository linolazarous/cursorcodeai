# ────────────────────────────────────────────────
# CursorCode AI - Production Environment (.env.production)
# Use this file on your production server / hosting platform
# Never commit real secrets to git!
# ────────────────────────────────────────────────

# === Database (Supabase PostgreSQL) ===
DATABASE_URL=postgresql://[user]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
# ^ Get this from Supabase → Settings → Database → Connection string (Direct connection, not pooled for migrations)

# === Redis (use Upstash or Render/Redis Labs) ===
REDIS_URL=redis://default:[password]@[host]:[port]/0
# Example Upstash: redis://default:abc123@us1-example.upstash.io:6379

# === xAI / Grok API ===
XAI_API_KEY=sk-...

# === Stripe (live keys in production) ===
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_...

# === SendGrid (email) ===
SENDGRID_API_KEY=SG....
EMAIL_FROM=no-reply@cursorcode.ai

# === JWT & Security ===
JWT_SECRET_KEY=your-very-long-random-secret-min-64-chars
JWT_REFRESH_SECRET=another-very-long-random-secret-min-64-chars
FERNET_KEY=your-32-byte-base64-encoded-fernet-key==

# === Application URLs ===
NEXT_PUBLIC_APP_URL=https://your-domain.com
FRONTEND_URL=https://your-domain.com

# === Auth (NextAuth / frontend) ===
NEXTAUTH_SECRET=long-random-secret-for-nextauth
NEXTAUTH_URL=https://your-domain.com

# === Environment & Logging ===
ENVIRONMENT=production
LOG_LEVEL=INFO               # or WARNING in very high traffic

# === Sentry (error monitoring) ===
SENTRY_DSN=https://[key]@o[org].ingest.sentry.io/[project]

# === Optional / Advanced ===
# PORT=8000                  # Render/Fly/Railway usually sets this automatically
# ALLOWED_HOSTS=your-domain.com,*.your-domain.com
# CORS_ORIGINS=https://your-domain.com,https://*.your-domain.com
