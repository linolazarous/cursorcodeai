# ╔════════════════════════════════════════════════════════════╗
# ║ CursorCode AI - Backend Production (.env.production)       ║
# ║ Use on your production server/hosting platform             ║
# ║ NEVER commit real secrets to git!                          ║
# ╚════════════════════════════════════════════════════════════╝

# === Database (Supabase PostgreSQL - Direct connection) ===
DATABASE_URL=postgresql://[postgres]:[YOUR-PASSWORD]@db.[project-ref].supabase.co:5432/postgres
# ^ Copy DIRECT connection string from Supabase → Settings → Database
# Do NOT use pooled (port 6543) for migrations or heavy writes

# === Redis (Upstash recommended) ===
REDIS_URL=redis://default:[password]@[host]:[port]/0
# Example Upstash: redis://default:abc123@us1-example.upstash.io:6379

# === xAI / Grok API ===
XAI_API_KEY=sk-...

# === Stripe (LIVE keys for production) ===
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY is for frontend only

# === SendGrid (email notifications) ===
SENDGRID_API_KEY=SG....
EMAIL_FROM=no-reply@cursorcode.ai

# === JWT & Security ===
JWT_SECRET_KEY=your-very-long-random-secret-min-64-chars
JWT_REFRESH_SECRET=another-very-long-random-secret-min-64-chars
FERNET_KEY=your-32-byte-base64-encoded-fernet-key==

# === Application URLs ===
NEXT_PUBLIC_APP_URL=https://your-domain.com              # Used in emails/links
FRONTEND_URL=https://your-domain.com                     # Same as above

# === Environment & Logging ===
ENVIRONMENT=production
LOG_LEVEL=INFO                           # or WARNING in very high traffic

# === Custom Monitoring (optional) ===
# MONITORING_ENDPOINT=https://your-logging-service.com    # if using external log service

# === Optional / Advanced ===
# PORT=8000                                      # Usually set by hosting platform
# ALLOWED_HOSTS=your-domain.com,*.your-domain.com
# CORS_ORIGINS=https://your-domain.com,https://*.your-domain.com
